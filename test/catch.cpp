// This file exists solely as a place to build the
// catch main(). This is done to speed up
// iteration times when changing the tests

#define CATCH_CONFIG_MAIN // This tells Catch to provide a main() - only do this in one cpp file
#include <catch2/catch.hpp>

